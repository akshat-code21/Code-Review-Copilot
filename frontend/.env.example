# Backend API base URL used by the frontend fetch client.
# In local dev this is left unset so Vite proxies /api to localhost:8000.
# For Vercel production, set this to the deployed backend origin.
VITE_API_BASE_URL=https://code-review.spoo.me/api/v1
